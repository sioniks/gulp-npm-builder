mixin title (title, url, additionClass)
	.title(style=`background-image: url('img/decor/${url}')` class=additionClass)= title

mixin button (name, href)
	- href = href || "#"
	a.button(href=href)= name